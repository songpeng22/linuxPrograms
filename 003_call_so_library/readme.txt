# cd library

# make

# cd main

# make

# ./main

# if run on android with g++ build, need to copy libc++_shared.so from /opt/android-ndk/android-ndk-r23b/toolchains/llvm/prebuilt/linux-x86_64/sysroot/usr/lib/aarch64-linux-android/libc++_shared.so
